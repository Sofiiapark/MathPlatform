{
  "extends": "../tsconfig.base.json", // Розширюємо загальний файл
  "include": [
    "src/**/*",         // Включає файли цього сервісу
    "../shared/**/*" // Компілюємо лише файли цього сервісу
  ],
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "../"
  }
}